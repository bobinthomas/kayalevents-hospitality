export function LockedBanner() {
  return (
    <div className="rounded border border-gold/40 bg-gold/10 p-4 text-sm text-gold-bright">
      This form is now read-only — the deadline has passed (or it has been locked). Contact
      the Kayal Events team if you need to make a change.
    </div>
  );
}
